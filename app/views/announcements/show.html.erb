<p style="color: green"><%= notice %></p>

<%= render @announcement %>

<div>
  <%= link_to "Edit this announcements", edit_announcement_path(@announcement),  class: "edit-link" %> |
  <%= link_to "Back to announcements", announcements_path, class: "back-link" %>

  <%= button_to "Destroy this announcements", @announcement, method: :delete , class: "destroy-button", data: { turbo_method: 'delete', turbo_confirm: "Are you sure?" }%>
</div>
