<div id="<%= dom_id announcement %>">
  <div class="card" style="color: #4D2D18;">
    <p>
      <strong>Member name:</strong>
      <%= announcement.member.name %>
    </p>

    <p>
      <strong>Title</strong>
      <%= announcement.title %>
    </p>

    <p>
      <strong>Description</strong>
      <%= announcement.description %>
    </p>
  </div>

</div>
